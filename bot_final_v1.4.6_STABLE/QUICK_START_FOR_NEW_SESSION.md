# ⚡ БЫСТРЫЙ СТАРТ ДЛЯ НОВОЙ СЕССИИ CLAUDE

**Используй этот файл при загрузке проекта в новую сессию!**

---

## 📁 ЧТО ЭТО ЗА ПРОЕКТ?

**Hybrid Trading Bot v1.4.1** — полностью автоматизированный торговый бот для BTC/USDT на бирже BingX.

**Основные характеристики:**
- Биржа: BingX, Cross Margin
- Актив: BTC/USDT Perpetual
- Плечо: 20x
- Режимы: TREND / RANGE (автоопределение)
- Стратегия: Адаптивная DCA сетка + динамический TP

---

## 📂 СТРУКТУРА ФАЙЛОВ (ВАЖНО!)

```
bot_final_v1.4.1/
├── main.py                           # ТОЧКА ВХОДА - запускать отсюда
├── trading_bot.py                    # ОСНОВНОЙ КОД (71KB, 2428 строк)
├── config.py                         # Все настройки
├── telegram_bot.py                   # Telegram интеграция
├── ai_assistant.py                   # AI помощник (Gemini)
├── security.py                       # Шифрование API ключей
│
├── README.md                         # Руководство пользователя
├── FUNCTIONS_REFERENCE.md            # ⭐ НАЧНИ С ЭТОГО!
├── CHANGELOG.md                      # История версий
├── requirements.txt                  # Зависимости
│
└── docs/
    ├── BOT_LOGIC_DOCUMENTATION.md    # Полная техническая документация
    ├── GRID_ANALYSIS_REPORT.md       # Детальный анализ сетки
    └── grid_calculator.py            # Калькулятор для расчетов
```

---

## 🎯 ЧТО ЧИТАТЬ В ПЕРВУЮ ОЧЕРЕДЬ?

### 1. **FUNCTIONS_REFERENCE.md** (ОБЯЗАТЕЛЬНО!)
- Список всех 39 функций бота
- Краткое описание каждой
- Ключевые формулы
- Известные проблемы
- **Время чтения: 10-15 минут**

### 2. **BOT_LOGIC_DOCUMENTATION.md** (для глубокого понимания)
- Полная техническая документация (927 строк)
- Все системы с формулами
- Main Loop
- **Время чтения: 30-40 минут**

### 3. **GRID_ANALYSIS_REPORT.md** (для понимания рисков)
- 4 детальных сценария
- Запас прочности по режимам
- **Время чтения: 10 минут**

---

## ⚡ БЫСТРАЯ ОРИЕНТАЦИЯ В КОДЕ

### Основные файлы:

#### `trading_bot.py` (ГЛАВНЫЙ!)
**Основные функции:**
- `check_entry_signal_hybrid()` — проверка 7 фильтров + confluence
- `open_position()` — открытие позиции
- `place_limit_dca()` — размещение DCA (с умным множителем!)
- `execute_dca()` — обработка исполненного DCA
- `place_limit_tp()` — TP (обновляется после каждого DCA)
- `check_trailing_stop()` — trailing stop
- `close_position_market()` — закрытие позиции
- `run()` — главный цикл

**Ключевые алгоритмы:**
- `get_smart_distance_multiplier()` — умный множитель DCA (ATR × RSI × GEO)
- `calculate_smart_position_size_hybrid()` — адаптивный размер входа
- `get_dynamic_tp_steps()` — динамический TP от ATR

#### `config.py`
**Все константы:** leverage, размеры входа, DCA дистанции, веса, фильтры, лимиты

---

## 🔑 КЛЮЧЕВЫЕ КОНЦЕПЦИИ

### 1. Confluence Scoring (0-7 баллов)
- Оценка силы сигнала по 7 факторам
- Определяет стадию входа: Stage 1 (0-2), Stage 2 (3-4), Stage 3 (5-7)
- Чем выше confluence → тем больше размер позиции

### 2. Smart Distance Multiplier
```python
multiplier = (ATR / 0.002) × RSI_factor × (1.1 ^ safety_count)
```
- Адаптирует дистанции DCA под волатильность и перепроданность
- Больше волатильность → дальше DCA → меньше ложных срабатываний

### 3. TREND vs RANGE режимы
- TREND (ADX > 25): плотная сетка, меньшие веса
- RANGE (ADX ≤ 25): широкая сетка, большие веса
- Автоопределение каждый цикл

### 4. Двойной стоп-лосс
- **Биржевой ордер**: backup (НЕ обновляется после DCA)
- **Программная проверка**: main loop (РАБОТАЕТ, обновляется)
- SL срабатывает при убытке = 30% баланса

### 5. Запас прочности
- TREND низкая вол.: -8.88% от входа до SL
- TREND высокая вол.: -14.57% от входа до SL
- RANGE средняя вол.: -11.38% от входа до SL
- RANGE экстрим: -22.99% от входа до SL

---

## ⚠️ ИЗВЕСТНЫЕ ОСОБЕННОСТИ (НЕ БАГИ!)

### 1. DCA5 может быть НИЖЕ стоп-лосса
**Это защита!** При высокой волатильности SL срабатывает РАНЬШЕ чем DCA5.
Предотвращает усреднение в экстремальное падение.

### 2. SL ордер НЕ обновляется после DCA
**Это нормально!** Программная проверка в main loop работает корректно.
Биржевой SL — backup на случай обрыва связи.

### 3. "После SL еще X% до ликвидации"
**НЕПРАВИЛЬНАЯ формулировка!** После SL позиции НЕТ.
Ликвидация — резервная защита при flash crash (когда SL не успел).

---

## 🔍 ГДЕ ИСКАТЬ ЧТО

### Логика входа:
- `check_entry_signal_hybrid()` — 7 фильтров
- `calculate_confluence_score()` — оценка силы
- `calculate_smart_position_size_hybrid()` — размер позиции

### DCA:
- `get_dca_parameters()` — выбор TREND/RANGE параметров
- `get_smart_distance_multiplier()` — расчет множителя
- `place_limit_dca()` — размещение ордера
- `execute_dca()` — обработка исполнения

### Выход:
- `get_dynamic_tp_steps()` — динамический TP
- `place_limit_tp()` — размещение TP
- `check_trailing_stop()` — проверка trailing
- `close_position_market()` — закрытие

### Защита:
- Программная проверка SL — в `run()` main loop
- `_sync_position_with_exchange()` — синхронизация с биржей
- PnL Audit — в `run()` main loop

---

## 🚨 КРИТИЧЕСКИЕ МОМЕНТЫ

### 1. Средняя цена после DCA
```python
# ВЗВЕШЕННАЯ средняя!
prev_total_usd = avg_price × (total_size_btc - dca_size_btc)
dca_usd = dca_price × dca_size_btc
avg_price = (prev_total_usd + dca_usd) / total_size_btc
```

### 2. Направление DCA
```python
# LONG: DCA НИЖЕ входа
dca_price = base_entry_price × (1 - actual_dist)

# SHORT: DCA ВЫШЕ входа (✅ исправлено в v1.3)
dca_price = base_entry_price × (1 + actual_dist)
```

### 3. Обновление после DCA
```python
execute_dca():
    # Обновляет:
    - safety_count += 1
    - total_size_coins += fill_amount
    - avg_price (взвешенная!)
    - current_trade_fees += dca_fee

    # Вызывает:
    - place_limit_tp()  ✅ TP обновляется!
    - place_limit_dca() ✅ Следующий DCA

    # НЕ вызывает:
    - place_stop_loss() ❌ SL остается фиксированным
```

### 4. Ликвидация (BingX Cross Margin)
```python
MMR = 0.5%
maintenance_margin = position_size_usd × MMR
max_loss = balance - maintenance_margin

# После DCA5 (TREND):
# position = $18,604
# mm = $93
# max_loss = $3,400 - $93 = $3,307 (97% баланса!)

# Стоп-лосс: -$1,020 (30% баланса)
# Запас: $3,307 - $1,020 = $2,287 ✅
```

---

## 📋 ЧЕКЛИСТ ДЛЯ АНАЛИЗА ПРОБЛЕМЫ

Если пользователь сообщает о проблеме:

### 1. Определи версию
- [ ] Это v1.4.1? (проверь `trading_bot.py` размер ~71KB)
- [ ] Есть ли последние фиксы? (SHORT DCA, ID сравнение)

### 2. Проверь логи
- [ ] `bot_hybrid.log` — основные события
- [ ] `blackbox_*.json` — детальная история
- [ ] `trades_hybrid.csv` — статистика

### 3. Определи проблему
- [ ] Ложный вход? → Смотри confluence и фильтры
- [ ] Не сработал DCA? → Проверь Smart Multiplier
- [ ] Странный TP? → Проверь Dynamic TP формулу
- [ ] Ликвидация? → Проверь маржу и расчет
- [ ] SL не сработал? → Проверь программную проверку в main loop

### 4. Проверь параметры
- [ ] `config.py` — все константы
- [ ] Режим рынка: TREND или RANGE?
- [ ] Confluence: сколько баллов было?
- [ ] Stage: какая стадия входа?

---

## 💡 БЫСТРЫЕ ОТВЕТЫ НА ЧАСТЫЕ ВОПРОСЫ

**Q: Почему DCA5 ниже стоп-лосса?**
A: Защита! SL срабатывает РАНЬШЕ при экстремальной просадке.

**Q: Почему стоп-лосс не обновляется после DCA?**
A: Программная проверка работает. Биржевой SL — backup.

**Q: Сколько маржи используется?**
A: 27-30% после всех 5 DCA. Остальное свободно.

**Q: Как работает Smart Multiplier?**
A: `(ATR/0.002) × RSI_factor × 1.1^safety_count`

**Q: Когда бот входит в позицию?**
A: Когда прошел ВСЕ 7 фильтров + confluence >= 1.

---

## 🚀 БЫСТРЫЙ СТАРТ АНАЛИЗА

```python
# 1. Прочитай FUNCTIONS_REFERENCE.md (10 мин)
# 2. Открой trading_bot.py
# 3. Найди функцию run() — это main loop
# 4. Проследи логику:
#    - Если нет позиции → check_entry_signal_hybrid()
#    - Если есть позиция → проверки DCA, TP, Trailing, SL
# 5. Изучи ключевые функции:
#    - get_smart_distance_multiplier()
#    - execute_dca()
#    - place_limit_tp()
```

---

**Готово!** Теперь ты знаешь, с чего начать! 🎯

**Следующий шаг**: Прочитай `FUNCTIONS_REFERENCE.md` и начинай работу!
