# 🤖 BOT MODULES - Модульная структура торгового бота

## Описание
Модульная архитектура для Hybrid Trading Bot v1.4.5

## Структура модулей

### 📦 `__init__.py`
Точка входа для пакета модулей. Экспортирует основной класс `HybridTradingBot`.

### 🔧 `bot_core.py`
**Базовый модуль с инициализацией и утилитами**

Содержит:
- Класс `HybridTradingBot` с полной инициализацией
- Утилитарные методы:
  - `log()` - Цветное логирование
  - `log_debug()` - Отладочное логирование
  - `get_effective_balance()` - Расчёт доступного баланса
  - `get_current_pnl()` - Получение PnL с биржи
  - `refresh_wallet_status()` - Обновление баланса

Переменные состояния:
- **Позиция**: `in_position`, `position_side`, `avg_price`, `total_size_coins`
- **Ордера**: `tp_order_id`, `dca_order_id`, `sl_order_id`
- **Трейлинг**: `trailing_active`, `range_trailing_enabled`
- **Статистика**: `session_total_pnl`, `session_wins`, `session_losses`
- **Защита**: `close_attempt_count`, `max_close_attempts`

### 📊 `analytics.py`
**Аналитика и мониторинг**

Методы:
- `log_blackbox()` - JSON логирование всех событий
- `check_pnl_audit()` - Проверка соответствия PnL
- `start_future_spy()` - Мониторинг "что было бы если..."

## Планируемые модули

### 📈 `market_analysis.py`
Анализ рынка и генерация сигналов:
- `get_market_data_enhanced()`
- `calculate_confluence_score()`
- `check_entry_signal_hybrid()`

### 💼 `position_manager.py`
Управление позициями:
- `open_position()`
- `close_position()`
- `calculate_smart_position_size_hybrid()`

### 📋 `order_manager.py`
Управление ордерами:
- `place_limit_tp()`
- `place_limit_dca()`
- `place_stop_loss()`
- `cancel_all_orders()`

### 🛡️ `risk_manager.py`
Управление рисками:
- `calculate_danger_level()`
- `check_safety_for_dca_return()`
- `get_dynamic_tp_steps()`
- `get_dca_parameters()`

### 📱 `telegram_handler.py`
Telegram интеграция:
- `update_dashboard()`
- `check_telegram_commands()`
- `send_or_update_trade_message()`

### 🤖 `ai_integration.py`
AI аналитика:
- `trigger_ai_report_thread()`
- `trigger_ai_chat_reply()`
- `perform_health_check()`

## Использование

```python
from bot_modules import HybridTradingBot

# Создание бота
bot = HybridTradingBot(exchange, telegram_bot)

# Запуск основного цикла
bot.run()
```

## Преимущества модульной архитектуры

1. **Читаемость** - Код разделён на логические блоки
2. **Поддержка** - Легче находить и исправлять баги
3. **Тестирование** - Каждый модуль можно тестировать отдельно
4. **Расширяемость** - Легко добавлять новые функции
5. **Переиспользование** - Модули можно использовать в других проектах

## Совместимость

Модули полностью совместимы с оригинальным файлом `trading_bot.py`.
Можно постепенно переносить функционал в модули без остановки работы бота.

## Версия
v1.4.5 - Критические исправления и защита от зависаний

## Автор
Hybrid Trading Bot Team
