# 🎯 BOT POSITIONS MODULE

Модуль для управления позициями торгового бота.

## 📦 Класс: BotPositionsMixin

Миксин, предоставляющий методы для работы с позициями на бирже.

### Методы:

#### 1. `reset_position()`
**КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ БАГ #4 v1.4.5**

Сброс состояния позиции после закрытия.

**Что сбрасывается:**
- Флаги позиции (in_position, position_side)
- Цены (avg_price, first_entry_price, base_entry_price)
- Размеры (total_size_coins, entry_usd_vol)
- Счетчики DCA (safety_count)
- Защита DCA (drawdown, multipliers, history)
- Трейлинг (trailing_active, peak_price, range_trailing)
- Ордера (tp_order_id, sl_order_id, dca_order_id)

**Пример использования:**
```python
self.reset_position()  # Сброс после закрытия позиции
```

#### 2. `wait_for_order_fill(order_id, timeout=30)`

Ожидание исполнения ордера с таймаутом.

**Параметры:**
- `order_id` (str): ID ордера на бирже
- `timeout` (int): Таймаут в секундах (по умолчанию 30)

**Возвращает:**
- `tuple`: (success: bool, fill_price: float)
  - success: True если ордер исполнен
  - fill_price: Средняя цена исполнения (0 если не исполнен)

**Пример использования:**
```python
success, price = self.wait_for_order_fill(order['id'], timeout=30)
if success:
    print(f"Order filled at {price}")
else:
    print("Order not filled")
```

#### 3. `_sync_position_with_exchange()`
**ИСПРАВЛЕНО v1.4.2**

Синхронизация позиции с биржей.

**Критические исправления:**
- Используем positionSide из API (для BingX)
- BingX возвращает ПОЛОЖИТЕЛЬНОЕ amount даже для SHORT
- Восстанавливаем base_entry_price, entry_usd_vol
- Примерно определяем safety_count из размера позиции
- Сбрасываем защиту DCA если позиция отсутствует

**Пример использования:**
```python
self._sync_position_with_exchange()  # Синхронизация после операций
```

#### 4. `open_position_limit(signal_data, df)`

Открытие позиции лимитным ордером.

**Параметры:**
- `signal_data` (dict): Словарь с сигналом
  ```python
  {
      'signal': 'Buy' | 'Sell',
      'stage': 1-3,  # Стадия входа
      'confluence': 0-7  # Количество совпавших индикаторов
  }
  ```
- `df` (DataFrame): DataFrame с рыночными данными

**Алгоритм:**
1. Проверка существующих позиций на бирже
2. Расчет размера позиции (smart position sizing)
3. Размещение limit ордера по bid/ask цене
4. Ожидание исполнения (30 сек)
5. Обновление состояния бота
6. Размещение TP, DCA, SL ордеров
7. Активация Range Trailing для боковых рынков

**Критические исправления v1.4.5:**
- Сброс счетчика попыток закрытия
- Активация многоуровневой Range Trailing защиты

**Пример использования:**
```python
signal = {
    'signal': 'Buy',
    'stage': 2,
    'confluence': 5
}
self.open_position_limit(signal, market_df)
```

#### 5. `close_position_market(reason)`
**КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ БАГ #8 v1.4.5**

Закрытие позиции market ордером с защитой от бесконечного цикла.

**Параметры:**
- `reason` (str): Причина закрытия (для логов)

**Алгоритм:**
1. Отмена всех активных ордеров (TP, DCA, SL)
2. Размещение market ордера на закрытие
3. Расчет PnL (gross и net с комиссиями)
4. Обновление баланса и статистики
5. Запись в CSV лог
6. Blackbox логирование
7. Future Spy (мониторинг после выхода)
8. Сброс защиты DCA и счетчиков

**Защита от бесконечного цикла (v1.4.5):**
- Счетчик попыток закрытия (max 3)
- При превышении лимита:
  - Отключение трейлинга
  - TP ордер продолжает работать
  - Уведомление в Telegram о критической ошибке

**Критические исправления:**
- 🔥 БАГ #5: Убрали reduceOnly для BingX Hedge режима
- 🔥 БАГ #8: Защита от бесконечных попыток

**Пример использования:**
```python
self.close_position_market("TP Hit")  # Закрытие по тейк-профиту
self.close_position_market("SL Hit")  # Закрытие по стоп-лоссу
self.close_position_market("Manual")  # Ручное закрытие
```

## 🔥 Критические исправления v1.4.5

### БАГ #4: Отсутствующий метод reset_position()
**Проблема:** Метод вызывался в коде, но не существовал!

**Решение:** Добавлен метод `reset_position()` с полным сбросом состояния.

### БАГ #5: Ошибка с reduceOnly в BingX Hedge режиме
**Проблема:** BingX в Hedge режиме не поддерживает параметр reduceOnly.

**Решение:** Убран параметр reduceOnly, используется только positionSide.

### БАГ #8: Бесконечный цикл при ошибках закрытия
**Проблема:** При ошибках закрытия бот зацикливался на попытках.

**Решение:** Добавлена система защиты:
- Счетчик попыток (max 3)
- Автоматическое отключение трейлинга при превышении
- TP ордер продолжает работать
- Уведомления в Telegram

## 🔧 Использование

### Базовый пример:
```python
from bot_modules import BotPositionsMixin

class MyBot(BotPositionsMixin):
    def __init__(self):
        # Инициализация
        pass
    
    def trade(self):
        # Открытие позиции
        signal = {'signal': 'Buy', 'stage': 2, 'confluence': 5}
        self.open_position_limit(signal, df)
        
        # ... торговля ...
        
        # Закрытие позиции
        self.close_position_market("TP Hit")
        
        # Сброс состояния
        self.reset_position()
```

## 📊 Зависимости

Миксин предполагает наличие следующих атрибутов в классе:
- `self.exchange` - объект биржи (ccxt)
- `self.symbol` - торговая пара
- `self.in_position` - флаг наличия позиции
- `self.position_side` - направление позиции ('Buy'/'Sell')
- `self.avg_price` - средняя цена входа
- `self.total_size_coins` - размер позиции в монетах
- `self.safety_count` - количество DCA уровней
- `self.entry_usd_vol` - объем входа в USD
- `self.close_attempt_count` - счетчик попыток закрытия
- `self.max_close_attempts` - максимум попыток закрытия

И методов:
- `self.log()` - логирование
- `self.refresh_wallet_status()` - обновление баланса
- `self.calculate_smart_position_size_hybrid()` - расчет размера позиции
- `self.get_effective_balance()` - получение эффективного баланса
- `self.get_real_order_fee()` - получение реальной комиссии
- `self.send_or_update_trade_message()` - отправка сообщений
- `self.place_limit_tp()` - размещение TP ордера
- `self.place_limit_dca()` - размещение DCA ордера
- `self.place_stop_loss()` - размещение SL ордера
- `self.reset_trailing()` - сброс трейлинга
- `self.log_blackbox()` - blackbox логирование
- `self.start_future_spy()` - запуск future spy
- `self.update_dashboard()` - обновление дашборда
- `self.cancel_all_orders()` - отмена всех ордеров
- `self.get_dca_parameters()` - получение параметров DCA

## 📝 История версий

### v1.4.5 (2026-01-27)
- 🔥 Добавлен метод reset_position() (БАГ #4)
- 🔥 Защита от бесконечного цикла в close_position_market() (БАГ #8)
- 🔥 Убран reduceOnly для BingX (БАГ #5)

### v1.4.2
- 🔧 Range Trailing для боковых рынков
- 🔧 Правильное определение position_side из API
- 🔧 Детальное логирование синхронизации

### v1.4.1
- Детальное логирование операций
